/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Players} from './players.model';

describe('Players', () => {
  it('should create an instance', () => {
    expect(new Players()).toBeTruthy();
  });
});
