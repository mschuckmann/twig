/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Shots} from './shots.model';

describe('Shots', () => {
  it('should create an instance', () => {
    expect(new Shots()).toBeTruthy();
  });
});
