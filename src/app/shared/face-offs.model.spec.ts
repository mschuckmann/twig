/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {FaceOffs} from './face-offs.model';

describe('FaceOffs', () => {
  it('should create an instance', () => {
    expect(new FaceOffs()).toBeTruthy();
  });
});
